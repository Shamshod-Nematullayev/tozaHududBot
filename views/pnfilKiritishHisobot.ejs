<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Document</title>
    <style>
      body {
        font-family: sans-serif;
        font-size: 20px;
      }
      table {
        overflow: hidden;
        border-collapse: collapse;
        padding: 10px;
      }
      h2 {
        text-align: center;
      }
      h3 {
        margin: 5px;
        line-height: 15px;
      }
      div {
        display: inline-block;
        position: relative;
      }
      th,
      td {
        border: 1px solid black;
        padding: 2px 5px;
        margin: 0;
        border-collapse: collapse;
        font-weight: bold;
      }
      td {
        text-align: right;
      }
      p {
        text-decoration: underline;
      }
      th {
        background-color: rgb(202, 151, 253);
      }
      td:last-child {
        text-align: right;
      }
      .left {
        text-align: left;
      }
      #oliy_ong {
        position: absolute;
        top: 0;
        left: 0;
      }
    </style>
  </head>
  <body>
    <div>
      <i style="color: blue" id="oliy_ong">Oliy Ong</i>
      <h2>ПИНФЛ киритиш бўйича режа таҳлили</h2>
      <h3><%= sana %></h3>
      <!-- <h3>${newJSON.davr}</h3>-->
      <h3>Каттақўрғон туман / "ANVARJON BIZNES INVEST" MCHJ</h3>
      <table cellscasing="0">
        <tr>
          <th>№</th>
          <th style="width: 250px">Махалла</th>
          <th style="width: 120px">Режа</th>
          <th style="width: 120px">Жами киритилганлар</th>
          <th>Фоизда</th>
          <th style="width: 120px">Фарқи</th>
        </tr>
        <% data.forEach( (mfy, i) => { %>
        <tr>
          <td><%= i+1 %></td>
          <td align="left" class="left"><%= mfy.name %></td>
          <td>
            <%=
            Math.floor(mfy.shaxsi_tasdiqlandi_reja).toLocaleString().replace(/,/g,
            " ") %>
          </td>
          <td>
            <%= Math.floor(mfy.counterOfConfirm).toLocaleString().replace(/,/g,
            " ") %>
          </td>
          <td align="center"><%= mfy.bajarilishi_foizda %></td>
          <td>
            <%= Math.floor(mfy.farqi) .toLocaleString() .replace(/,/g, " ") %>
          </td>
        </tr>
        <% }) %> <% if(jamiReja && jamiKiritilgan){ %>
        <tr>
          <td></td>
          <td></td>
          <th>
            <%= Math.floor(jamiReja) .toLocaleString() .replace(/,/g, " ") %>
          </th>
          <th>
            <%= Math.floor(jamiKiritilgan) .toLocaleString() .replace(/,/g, " ")
            %>
          </th>
          <th align="center">
            <%= Math.round( (jamiKiritilgan / jamiReja + Number.EPSILON) * 1000
            ) / 10 %> %
          </th>
          <th>
            <%= Math.floor(jamiKiritilgan - jamiReja) .toLocaleString()
            .replace(/,/g, " ") %>
          </th>
        </tr>
        <% }%>
      </table>
      <footer><b>Бажарувчи: Шамшод Неъматуллаев</b></footer>
    </div>
  </body>
</html>
